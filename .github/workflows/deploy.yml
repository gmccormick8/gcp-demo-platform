# This is a basic workflow to help you get started with Actions

name: Terraform Deploy

on:
  push:
    branches: [ "prod" ]
  pull_request:
    branches: [ "prod" ]

  workflow_dispatch:


jobs:

  build:

    defaults:
      run:
        shell: bash
  
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name}}

    permissions:
      contents: 'read'
      id-token: 'write'

    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Cloud SDK
        uses: 'google-github-actions/setup-gcloud@v2'

      - id: 'auth-to-gcp'
        name: 'Authenticate to GCP'
        uses: 'google-github-actions/auth@v2.1.10'
        with:
            create_credentials_file: 'true'
            workload_identity_provider: '${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}'
            service_account: '${{ secrets.SERVICE_ACCOUNT }}'
      - id: 'run-gcloud-auth-login'
        name: 'gcloud auth login'
        run: |-
          gcloud auth login --brief --cred-file="${{ steps.auth-to-gcp.outputs.credentials_file_path }}" --force
          gcloud services list
          gcloud config set project ${{ vars.PROJECT_ID }}

      - id: 'setup-terraform'
        name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.12.1

      - id: 'terraform-fmt-check'
        name: Terraform Format Check
        run: terraform fmt -recursive

      - id: 'terraform-init'
        name: Initialize Terraform
        env:
          TF_VAR_project_id: ${{ vars.PROJECT_ID }}
          TF_VAR_service_account: ${{ secrets.SERVICE_ACCOUNT }}
        run: |
          terraform init \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}"

      - id: 'terraform-plan'
        name: Terraform Plan
        env:
          TF_VAR_project_id: ${{ vars.PROJECT_ID }}
          TF_VAR_service_account: ${{ secrets.SERVICE_ACCOUNT }}
        run: terraform plan