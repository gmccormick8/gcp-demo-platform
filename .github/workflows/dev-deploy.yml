name: Terraform Dev Deploy Pipeline

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-template.yml
    with:
      tfvars_file: dev.tfvars
    secrets: inherit